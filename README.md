# Сервер,  получает от клиента  команду Modbus TCP (порт 4545), отправляет её в локальный сериал-порт по протоколу Modbus      # RTU, получает от соответствующего устройства, подключенного к серверу по RS232/485,  Modbus RTU ответ и отсылает его клинту # по Modbus TCP . Используется  пакет своего репозитория github.com/Mikhail-go/mbus
# Запускает по таймеру опрос hold-регистров измерений  ОМ-310 и сохраняет все измеренные значения во внутреннем буфере с       # привязкой по времени
# Запускает свой http-сервер для отображения токов, напряжений, мощностей (по фазам) в виде svg-графиков в интервале от       # текущего момента времени обращения клиента минус заданный интервал в минутах до текущего времени.
